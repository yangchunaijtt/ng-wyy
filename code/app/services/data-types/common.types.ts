export type Banner = {
    targetId: number,
    imageUrl: string,
    url: string
}

export type SongSheet = {
    id: number,
    name: string,
    playCount: number,
    picUrl: string
}

export type Singer = {
    id: number,
    picUrl: string,
    name: string,
    briefDesc: string,
    albumSize: number
}